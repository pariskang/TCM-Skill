# tcm.py 与 GraphRAG 核心引擎:零依赖(Python 标准库 + SQLite FTS5)。
# 以下仅为「接入真实 LLM 后端」的可选依赖,按需安装其一即可;
# 默认 provider=rule 离线运行,无需任何依赖。

# 统一多后端路由(推荐:一装通吃 azure/openai/gemini/claude 等百余后端)
litellm>=1.40 ; extra == "litellm"

# openai SDK:azure / poe / openai / 任意 OpenAI 兼容端点 需要
openai>=1.30 ; extra == "openai"

# 说明:
#   azure  → 需 openai(AzureOpenAI 客户端)
#   poe    → 需 openai(OpenAI 兼容端点 https://api.poe.com/v1)
#   openai → 需 openai
#   litellm→ 需 litellm
# 安装示例:
#   pip install "litellm>=1.40"        # 用 litellm
#   pip install "openai>=1.30"         # 用 azure/poe/openai
